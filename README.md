# github-achievement-lab

This is a **learning repository** used to study how GitHub Issues, branches and pull requests work.

It was created with a small command line helper to practise the normal GitHub contribution workflow (open an issue, create a branch, open a pull request and merge it). All activity happens inside this repository, which belongs to its owner.

## Usage

Clone the repository and inspect the included examples.
